// Node server that handle Socket Io connections

const socketio  = require('socket.io')(8000)  //IMPORTING THE SOCKET.IO PACKAGE

const users = {};

socketio.on('connection',socket=>{
    socket.on('user-joined',name=>{
        users[socket.id] = name;
    })

})